#include <iostream>
#include <list>

int main() {
    std::list<int> ls;
    ls.push_back(1);
    ls.push_back(2);
    ls.push_back(3);

    ls.push_front(4);    //adds to the 1st position

    //REST ALL FUNCTIONS ARE SAME AS VECTOR 
}