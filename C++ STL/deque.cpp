#include <iostream>
#include <deque>

int main() {

    std::deque<int> q;
    q.push_back(1);
    q.push_back(1);
    q.push_back(1);
    q.push_back(1);

    q.pop_front();


    //REST ALL FUNCTIONS ARE SAME AS VECTOR 
}